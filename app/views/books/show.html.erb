<div>
    <h2><%= @book.title %></h2><br />
    <h3><%= @book.year %> - <%= @book.author %></h3><br />
    <h3><%= @book.classification %> - <%= @book.genre %> - <%= @book.book_type %>
</div>
<br />

<%= link_to 'Delete', book_path(@book), method: :delete, data: {confirm: 'Are you sure you want to delete this place?'}, class: 'btn btn-danger' %>

<%= link_to 'Edit', edit_book_path(@book), class: 'btn btn-primary' %>